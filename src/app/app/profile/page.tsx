// Profil route → the live, data-backed screen. The P1 fixture, the legacy page and
// the NEXT_PUBLIC_PROFILE_V2 flag were removed at the end of P4 (read path complete).
export { default } from "./ProfileV2";
